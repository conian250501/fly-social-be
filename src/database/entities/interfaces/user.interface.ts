export enum EGender {
  Male = "Male",
  Female = "Female",
  Other = "Other",
}

export enum EUserRole {
  User = "User",
  Admin = "Admin",
}

export enum EUserStatus {
  Active = "Active",
  InActive = "InActive",
}
